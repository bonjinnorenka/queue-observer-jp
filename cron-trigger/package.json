{
  "name": "queue-observer-cron-trigger",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers cron から collect.yml を workflow_dispatch で起動する",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "dev": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
